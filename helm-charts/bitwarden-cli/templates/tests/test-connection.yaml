apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bitwarden-cli.fullname" . }}-test-connection"
  labels:
    {{- include "bitwarden-cli.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bitwarden-cli.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
